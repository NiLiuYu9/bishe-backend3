<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.api.platform.mapper.UserTagMapper">

    <select id="selectTagNamesByUserId" resultType="java.lang.String">
        SELECT tag_name FROM user_tag WHERE user_id = #{userId}
    </select>

    <delete id="deleteByUserId">
        DELETE FROM user_tag WHERE user_id = #{userId}
    </delete>

    <delete id="deleteByUserIdAndTagName">
        DELETE FROM user_tag WHERE user_id = #{userId} AND tag_name = #{tagName}
    </delete>

    <select id="existsByUserIdAndTagName" resultType="java.lang.Integer">
        SELECT COUNT(1) FROM user_tag WHERE user_id = #{userId} AND tag_name = #{tagName}
    </select>

</mapper>
